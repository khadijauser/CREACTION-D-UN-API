// // config/config.js

// require('dotenv').config();

// const config = {
//   // Environnement (development, production, etc.)
//   env: process.env.NODE_ENV || 'development',

//   // Port du serveur
//   port: process.env.PORT || 5000,

//   // URI de connexion à MongoDB
//   MONGO_URL: process.env.MONGO_URL || 'mongodb://127.0.0.1:27017/api',

//   // Clé secrète pour JWT
//   jwtSecret: process.env.JWT_SECRET || 'jwt_super_secret_key_for_development',

//   // Durée de validité des tokens JWT
//   jwtExpiration: process.env.JWT_EXPIRATION || '24h',

//   // Clé secrète pour les sessions express-session
//   sessionSecret: process.env.SESSION_SECRET || 'session_super_secret_key_for_development',

//   // URL du client autorisé (pour CORS)
//   clientURL: process.env.CLIENT_URL || 'http://localhost:5000'
// };

// module.exports = config;
